apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: humans.agentteams.io
spec:
  group: agentteams.io
  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [permissionLevel]
              properties:
                displayName:
                  type: string
                  description: Friendly display name (Matrix profile displayname and listings). Empty leaves the Matrix profile displayname unset.
                username:
                  type: string
                  description: Matrix localpart for this human (fallback to metadata.name)
                email:
                  type: string
                  format: email
                permissionLevel:
                  type: integer
                  minimum: 1
                  maximum: 3
                  description: "1=Admin equivalent, 2=Team-scoped, 3=Worker-only (inclusive)"
                accessibleTeams:
                  type: array
                  items:
                    type: string
                  description: Teams this human can access (L2)
                accessibleWorkers:
                  type: array
                  items:
                    type: string
                  description: Standalone workers this human can access (L2/L3)
                identitySource:
                  type: object
                  nullable: true
                  properties:
                    issuer:
                      type: string
                    subject:
                      type: string
                  required: [issuer, subject]
                note:
                  type: string
                initialPassword:
                  type: string
                  description: "Custom initial Matrix password (legacy_password only); empty means the controller generates one and persists it into spec/status on creation"
            status:
              type: object
              properties:
                phase:
                  type: string
                  enum: [Pending, Active, Failed]
                matrixUserID:
                  type: string
                initialPassword:
                  type: string
                  description: "Generated password, shown once on creation"
                displayNameSyncedGeneration:
                  type: integer
                  format: int64
                rooms:
                  type: array
                  items:
                    type: string
                emailSent:
                  type: boolean
                message:
                  type: string
                observedGeneration:
                  type: integer
                  format: int64
                  description: "Most recent generation observed by the controller. Mirrors observedGeneration on Worker/Team/Manager status."
                consecutiveFailures:
                  type: integer
                  description: "Consecutive reconcile failures driving failHuman's exponential backoff. Reset to 0 on a successful pass."
                maxRetriesReached:
                  type: boolean
                  description: "True once maxHumanRetries consecutive failures stop automatic requeuing. Reset by setting the hiclaw.io/retry annotation."
                phaseTransitionTime:
                  type: string
                  format: date-time
                  description: "When the current Phase was last entered. Used by the backoff guard to skip passes before the backoff window elapses."
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Display Name
          type: string
          jsonPath: .spec.displayName
        - name: Level
          type: integer
          jsonPath: .spec.permissionLevel
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Email Sent
          type: boolean
          jsonPath: .status.emailSent
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
  scope: Namespaced
  names:
    plural: humans
    singular: human
    kind: Human
    shortNames: [hm]
