- feat(controller): Worker status 新增 `teamName`/`podName` 并上 kubectl 打印列（TEAM/POD，插在 Adapter 与 Phase 之间）——team 名复用 WorkerReconciler 既有每轮反查结果（`workerTeamName()`，annotation 优先 + list Teams），零新增查询；更新时机闭环：TeamReconciler 既有逻辑在 team 变化时给 member worker 打/删 `agentteams.io/team-name` annotation，天然触发 worker 重跑写入（建 team 即写、移出即清、standalone 恒空）；pod 名由 backend `WorkerResult.PodName` 顺路返回（k8s Create 带生成的 podName、Status 带 findWorkerPod 的 pod.Name，NotFound 清空），controller 域 pod=worker-bridge 的 bridge pod（`agentteams-worker-<w>-bridge`）/copaw 直接 pod，operator 供给的 runtime pod 恒 `<w>-cimicode` 规则可推导不在其内；status 纯展示不进 spec hash（不滚 pod），CRD 先后部署顺序不敏感（structural schema prune）
- feat(worker-bridge): 工具链配置增加配置文件回退通道 `/opt/teams/config/runtime.json`（interface-contract §1.1，v2.5）——stateless 沙箱平台无法在建沙箱时注入 env，改为平台侧服务按 operator 同款三源（bridge pod env 五键 + Worker CR `status.matrixUserID` + Team CR 反查）拼值写平铺 JSON（键=env 名原样，仅 `AGENTTEAMS_` 前缀字符串生效）；装载在 `mc_sync.load_runtime_config_file()`，**import mc_sync 时执行**（早于 `_MC_ALIAS` 模块级求值与 argparse default），taskflow/agentteams-sync/projectflow 三 CLI 零入口改动全覆盖；显式 env 恒优先（setdefault，pod 模式不受影响，文件缺席=正常态静默）；坏文件 stderr 警告后跳过不崩 CLI；文件更新后下一条命令即生效（每命令新进程）。mc_sync.py 四份同源副本（cli/taskflow、cli/projectflow、template task-management/file-sharing scripts）同步；cimicode-sandbox/cimicode-runtime 两 Dockerfile 契约注释同步
- refactor(worker-bridge): stateless 绑定四字段收拢为 `spec.runtimeParameter` 字符串 map（契约 v1.3）——Worker CR 删 `cimicodeGatewayUrl`/`sessionId`/`sandboxId`/`templateId` 四独立字段，改自由 map（camelCase 键惯例，追加平台参数不再改 CRD）；投影改嵌套 `bridge.runtimeParameter`（逐值 TrimSpace、blank 丢弃）；bridge 已知键（baseUrl/sessionId/sandboxId/templateId，camel/snake 双认）填固定字段，**追加键整袋平铺透传**进 stateless chat 请求体（固定字段之后 merge，平台忽略未知字段）；兼容读旧平铺 bridge 段（存量 MinIO runtime.yaml 不重写）；adapterMode 保持强类型独立分派键；hash 排除清单同步（runtimeParameter 不滚 pod）；Update API `*map[string]string` 三态（nil 不动/非 nil 整体替换）
- feat(worker-bridge): agent.md 协作段增加 Team Roster——`- <displayName|name> (<mxid>, <role>) — <description>`，自身按 matrix_id 排除、无 mxid 降级 name+role、空 description 省略尾巴；runtime.yaml members 的 displayName/description（controller 投影）为数据源，organization skill 同步指引
- fix(worker-bridge): 内网"收到 @mention 不回话"事故修复——接线改 bridge 自推导（契约 v1.2）。根因三层：①operator 在 bridge pod 创建 ~44s 后才 patch Worker CR env 两键，而 pod env 快照不可变、controller 对 spec.env 变化不滚动 pod；②自愈只在 gateway 缺失时启动，"Matrix 通、runtime client 缺"中间态无人接管；③accepted 消息遇 client 缺失被裸 return 静默丢弃。修复：裁决四态（显式 env > runtime.yaml bridge 段 > pod 模式命名推导 > 未定态）——svc 命名是契约级确定值 `<w>-cimicode-svc:4096`，晚到的只是可达性，`GET /session` 探测（<500 即就绪）通过才建 client；自愈三通道（bootstrap 重拉 / 推导健康等待 / controller env 兼容存量）15s 无限轮询，重建前过 gateway 运行守卫防双 sync；operator 删 ensure_worker_env（对 Worker CR 纯只读，存量两键同值无害）；start_background 在 client 缺失时也起自愈（ready 刻意保持 True 防崩溃循环）；accepted 消息丢弃可见化（warning + 即时唤醒自愈）。发版序：bridge 与 operator 必须同窗口升级（旧 bridge + 新 operator = 竞态回归）。
- feat(worker-bridge): cimicode-runtime 重写为内部 coder-cimicode 基础镜像形态——单端口 opencode REST（:4096），agent.md 改走消息体 `system` 字段原生通道，sandbox helper / bash.ts / 凭据烘焙（ZHIPU_API_KEY build-arg）全部退役；模型经 operator 注入 `OPENCODE_CONFIG_CONTENT`（镜像零凭据），`OPENCODE_PERMISSION={"*":"allow"}` 镜像 ENV 固化，`skills.paths` 零拷贝生效；emptyDir 移除（/workspace=容器可写层）
- feat(worker-bridge): operator 统一升级为 runtime 无关供给器——Worker spec.model + bridge pod 网关 env（AGENTTEAMS_AI_GATEWAY_URL/_WORKER_GATEWAY_KEY）渲染 agentteams-gateway provider 注入 Secret model-config（secretKeyRef，native-config 哨兵跳过；CIMICODE_MODEL_CONFIG_HASH 明文哈希变更触发滚动重启）；svc 收敛单端口、Deployment 去卷去 helper env，Worker env patch 收敛两键并显式清除存量残留 HELPER 键；模型链缺失本轮零副作用推迟（旧栈保留）
- feat(worker-bridge): 新增 opencode-runtime（node:22-slim + npm opencode-ai@1.18.27，与 cimicode-runtime 同契约——内外网切换只换运行时镜像）；vendored mc 上移 worker-bridge/bin 两 Dockerfile 共享；build-cimicode-runtime 改 `CIMICODE_BASE_IMAGE` 必填 + Makefile 守卫（内网 registry 地址不进外网仓库）；bridge 链 helper_url 全链退役（adapter/config/app/bootstrap/registry 五文件）
- fix(controller): pass pinned Human password into first Matrix provisioning and audit sso-login actions
